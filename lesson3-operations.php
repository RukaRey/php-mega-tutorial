<?php
    $x = 10;
    $y = 2;
    $z = null;

    // $z = $x + $y;   ADIÇÃO
    // $z = $x - $y;   SUBTRAÇÃO
    // $z = $x * $y;   MULTIPLICAÇÃO
    // $z = $x / $y;   DIVISÃO
    // $z = $x ** $y;  POTENCIA
    // $z = $x % $y;   RESTO


    // echo $z;

    $counter = 0;
    $step = 3;

    // $counter = $counter + 1; PLUS 1 
    // $counter++;              PLUS 1

    // $counter--;              MINUS 1
    
    // $counter += $step;       PLUS $step
    // $counter -= $step;       MINUS $step

    // echo $counter;



    /*ORDEM DAS OPERAÇÕES:
    PARENTESES ()
    POTENCIA   **
    DIVISÃO, MULTI, MOD * / %
    ADIÇÃO, SUBSTRAÇÃO + -
    */

    $operation = 2 * (3 + 1)**4 / 5 - 10;
    // echo "Complex operation: {$operation}";
?>